import CRAWL_TEST_MAP from '../const/form/TEST_CRAWL_MAP';
import reducerFactory from './Factory';

export default reducerFactory({
    map: CRAWL_TEST_MAP,
    name: 'testCrawl',
    state: {}
});